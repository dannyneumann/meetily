# Meetily direnv configuration

# Ensure node_modules/.bin is in path
PATH_add frontend/node_modules/.bin

# Add python venv to path if it exists
if [ -d "backend/venv" ]; then
  source backend/venv/bin/activate
fi

# Tauri specific environment variables if needed
# export TAURI_GPU_FEATURE=metal # example for macOS

echo "✅ Meetily environment loaded"
